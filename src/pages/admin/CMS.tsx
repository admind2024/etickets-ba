import { Settings } from "lucide-react";
import AdminPagePlaceholder from "@/components/AdminPagePlaceholder";

const CMS = () => {
  return <AdminPagePlaceholder title="CMS" icon={<Settings size={24} />} description="Content Management System" />;
};

export default CMS;
